# Bug Queue

## Fixed 2026-08-24 — Privilege escalation via user-account management (`AdminUserViewSet`)

**Critical finding from the RBAC hardening sprint** (see `docs/ARCHITECTURE_DECISIONS.md` AD-002,
`docs/SECURITY_HARDENING.md`), separate from and more serious than the same-day permission-gap fix above.

`AdminUserViewSet` (`backend/apps/administration/views.py`) required only `IsPropertyManager` for full
user-account management — create, update, deactivate, list. Its update path uses
`AdminUserCreateSerializer`, which has a writable `password` field. `is_superuser` was already read-only on
that serializer, so a Property Manager could not directly grant themselves superuser status — **but they
could reset any user's password, including a superuser's, via `PATCH /api/admin/users/<id>/`, then log in
as that account.** A live privilege-escalation path, unrelated to the `IsAuthenticated | IsPropertyManager`
no-op gap.

**Fixed** by changing `AdminUserViewSet.permission_classes` to `CanManageUsers` (`IsSuperAdmin`) — a
Property Manager must never reach user-account management. Regression tests added in
`backend/apps/administration/tests.py` (`test_users_property_manager_forbidden_*`, including a specific
password-reset test with a before/after hash comparison) and `backend/apps/core/tests.py`
(`AuthorizationMatrixTests`). Full backend suite: 281/281.

This sprint also introduced named, per-capability permission classes (`CanManageProperty`,
`CanManageOccupants`, `CanManageOccupancy`, `CanRecordPayment`, `CanManageUsers`, `CanViewAuditLog`,
`CanManageBackups` in `backend/apps/core/permissions.py`) replacing raw `IsPropertyManager`/`IsSuperAdmin`
references at each call site, and split `OccupancyViewSet.summary` out to `IsAuthenticated` (was
accidentally swept into the Property-Manager-only gate along with occupancy's write actions, which would
have broken the Dashboard/Overview page for every Staff-tier user).

## RC Final Stabilization Closeout (2026-07-04, see `docs/RELEASE_PLAN.md`)

Closes out the remaining items from the Release Candidate Verification Pass below: the one deferred destructive-action verification (Backup Restore) and the two deferred Medium/Low bugs. Nothing here is a new business feature; scope was strictly the four items called out in the closeout brief.

### Backup Restore — dry-run executed, verified production-safe
Previously verified by code review only (see "Not executed" note under the Verification Pass below). Executed live this pass against a fully isolated, disposable SQLite database (`DB_NAME` env override, never the real `backend/db.sqlite3`): ran migrations, seeded a realistic multi-entity dataset (2 properties, 3 sections, 4 units incl. one pricing rule, 5 occupants, 4 occupancies — one closed/checked-out, 3 payments with auto-generated receipts), created a backup, then simulated real corruption (field-level edits to a Property/Section/Payment/Student, hard-deletion of an unoccupied Unit, an unattached Student, and an Occupancy record), and restored. All 8 backed-up models (`Property`, `Section`, `Unit`, `PricingRule`, `Student`, `Occupancy`, `Payment`, `Receipt`) matched the pre-backup snapshot exactly afterward, including record counts, corrected field values, and both hard-deleted records being correctly recreated under their original primary keys. **Verdict: restore is production-safe** for the data it covers.

Two scope findings worth knowing before relying on it operationally (both by design, not bugs — no code changes made):
- **`AuditLog` and the `Backup` model itself are not part of the backup/restore payload.** A restore neither wipes nor recreates audit history, and never touches other `Backup` records — confirmed by the dry run (audit count unchanged after restore). This is the correct behavior (audit trails should stay immutable, backup history shouldn't be able to erase itself), but it means restoring does **not** roll back "who did what" — only the underlying business data.
- **`updated_at` timestamps (Django `auto_now=True`) are stamped with the restore's own execution time**, not the value in the backup file, because `auto_now` overrides on every `.save()` regardless of what the restore logic sets. Cosmetic only — no other field is affected — but worth knowing if `updated_at` is ever surfaced to end users as "last modified."
- Also noted operationally: `BackupService.BACKUP_DIR` is a fixed path (`settings.BASE_DIR / "backups"`) independent of which database is active, and `backend/backups/` already has ~350+ untracked `.json` files not covered by `.gitignore`. Not a restore-correctness issue; flagging for the upcoming Docker/DevOps pass (mount as a volume, and/or add to `.gitignore`).

### Archived Property Validation — fixed (closes the deferred Medium below)
`AdminService.create_section`/`create_unit` (`backend/apps/administration/services.py`) now reject creating a section under an archived property, or a unit under an archived section, raising the existing `ConflictError` with a friendly message ("Cannot add a section to '<name>' because it is archived. Reactivate the property first." / equivalent for units). Checking the immediate parent's `is_active` is sufficient for both cases: a property can only be archived once all its sections are already archived (existing `archive_property` guard), so blocking unit-creation on an archived *section* transitively blocks it under an archived *property* too — no extra property-level check needed on the unit path. Added `@_handle_exceptions` to `AdminSectionViewSet.create`/`AdminUnitViewSet.create` (previously only `update`/`archive` had it, since `create` never raised a service-level exception before this fix) so the `ConflictError` returns a proper `409` instead of an unhandled `500`. The frontend needed no changes — `AdminFormDialog`'s existing `onSave` try/catch already renders `err.message`, and the API client (`services/api.js`) already surfaces the response's `detail` field as `err.message`. Added `test_create_section_under_archived_property_rejected` and `test_create_unit_under_archived_section_rejected` to `backend/apps/administration/tests.py`. Files: `backend/apps/administration/services.py`, `backend/apps/administration/views.py`, `backend/apps/administration/tests.py`.

### UnitTab Status Badge — fixed (closes the deferred Low below)
Added a `statusLabels` map (`{active: 'Active', maintenance: 'Maintenance', archived: 'Archived'}`) alongside the existing `statusColors` map in `UnitTab` (`frontend/src/pages/Administration.jsx`) and used it for the badge's display text. The raw lowercase `u.status` value is untouched everywhere else (archive-button visibility check, API payloads), so no stored data or API behavior changed — display only.

### Reports — release decision documented
Confirmed and formally recorded as **Option A: deferred from v1.0, not release-blocking** (not a new decision — consistent with every prior pass calling it a pre-existing placeholder). See `docs/PROJECT_STATE.md` and `docs/RELEASE_PLAN.md` for the full statement and rationale.

## Release Candidate Verification Pass (full end-to-end workflow sweep, see `docs/RELEASE_PLAN.md`)

Verified live against a running instance (backend :8000, frontend :5173) across all 15 workflow areas in the RC verification brief, using a dedicated `demo` staff/superuser account and disposable `QARC`-prefixed test data (property, sections, units, 3 occupants, occupancies, payments) created, exercised, and cleaned up afterward.

### High
- BUG-026 — **Layer: Service.** Occupant search (`StudentService.list_students`, `apps/occupants/services/__init__.py`) matched the raw search string independently against each field (`first_name`, `last_name`, `phone`, `student_id_number`, `national_id`) via `icontains`. Searching a person's **full name** (e.g. "QarcOcc Bravo") returned **zero results**, because no single field contains both the first and last name together — only single-word searches (first name alone, or last name alone) worked. This breaks the single most natural search pattern for the target non-technical user persona (searching by full name, exactly what the placeholder text "Search by name, phone, ID..." invites) and is release-blocking for the explicitly-listed "Search occupant" workflow. Reproduced live, fixed by splitting the query into whitespace-separated tokens and requiring each token to match at least one field (AND across tokens, OR across fields per token) — identical behavior for single-word searches, correct behavior for multi-word full-name searches. File: `backend/apps/occupants/services/__init__.py`.
- BUG-027 — **Layer: Service.** The exact same independent-field-matching bug existed in Payments search (`PaymentService.list_payments`, `apps/payments/services/__init__.py`), used by the Payments page's "Search occupant or reference..." box. Fixed identically. Note: Receipts search was **not** affected — `Receipt.student_name` is a single denormalized field, so full-name search already worked there. File: `backend/apps/payments/services/__init__.py`.
- BUG-028 — **Layer: Frontend.** Dashboard's own "Quick Actions → Record Payment" button (`pages/Dashboard.jsx`) still navigated to the unrelated, unfiltered `/occupants` list — the exact dead-end pattern already fixed on the Payments page itself (BUG-021) was never applied to this second, separate entry point to the same action. Fixed by reusing the same `OccupantPickerDialog` + `RecordPaymentDialog` pair already built for Payments, so both "Record Payment" entry points now behave identically. File: `frontend/src/pages/Dashboard.jsx`.

### Medium (deferred — not fixed, per RC scope: only Critical/High are fixed)
- **Fixed 2026-07-04, see "RC Final Stabilization Closeout" at the top of this file.** Archived properties can still receive new sections (and, by extension, units) via Administration — the Sections/Units tabs' property dropdown does not exclude archived properties, and the create-section/unit forms have no guard against it. Reproduced live: created a section under a freshly-archived property with no warning. Not release-blocking (an owner is unlikely to add child records to a property they just archived), but a real logical gap. Would require new validation in `SectionService`/`UnitService` plus a frontend dropdown filter — deferred as a follow-up, not a small/safe fix.
- **Fixed 2026-07-04, see "RC Final Stabilization Closeout" at the top of this file.** `UnitTab`'s status badge (Administration → Units) renders the raw enum value in lowercase ("active", "maintenance", "archived") instead of Title Case, unlike every other status badge in the app (Active/Archived/Inactive). Purely cosmetic. File: `frontend/src/pages/Administration.jsx`.

### Verified clean (reproduced live, no bug found)
- Full auth cycle: login (valid + invalid credentials with correct error copy), logout, session persistence across a hard refresh, protected-route redirect both before login and after logout.
- Property create/edit/archive, with the Active/Archived badge (BUG-016 fix) confirmed correctly reflecting real state end-to-end.
- Section create/rename; Unit create with capacity/pricing, and Active ⇄ Maintenance ⇄ Active status transitions.
- Property Explorer: active properties/sections/units display correctly with live occupancy badges; archived properties correctly excluded (already-documented, pre-existing behavior — see prior "Discovered while fixing BUG-016" note); unit detail panel renders correct data; "Assign Occupant" quick action reachable from an empty unit.
- Occupant register/search/edit/profile view/archive.
- Occupancy: assignment wizard (Property → Section → Unit), **over-capacity assignment correctly blocked** (unit shows "Full" and is disabled once at capacity), checkout, and occupancy history preserved after checkout (closed record retains start/end dates and billing mode). "Move to another unit" is **not implemented** (no route, no service method) — matches the known gap vs. `docs/WORKFLOWS.md`'s aspirational spec; not built here per "no new business features."
- Payments: full payment, partial payment (correct running balance: 600,000 → 400,000 → 0), monthly-billing payment (correctly priced off `monthly_price` rather than `semester_price`), payment history, and financial-rule validation (zero and negative amounts both rejected client-side and server-side via `validate_positive`).
- Receipts: generated automatically per payment, searchable, PDF endpoint returns a valid response with no error.
- Dashboard: occupancy/outstanding/recent-payments figures update live as data changes; Recent Payments now shows real property names (BUG-022, prior session) confirmed working against genuinely new data, with "Property not specified" confirmed as the correct fallback for pre-existing payments recorded before a unit was ever assigned.
- Audit Trail: every action taken during this pass (create/edit/archive property, section, unit; register/edit/checkout occupant; assign occupancy; record payment) appears with correct actor (`demo`), action, entity, description, and timestamp.
- Backup & Export: Create Backup and Backup History both work (new backup appeared immediately with correct size/record count); Export Data produces a real file download (`occupants.csv`).
- Responsive UI: zero horizontal page overflow across Overview, Occupants, Payments, Receipts, Explorer, and Administration at 1280px/768px/375px (18/18 checks clean), consistent with the responsive pass in a prior session.

### Not executed (destructive, by design — see `docs/RELEASE_PLAN.md`)
- **Backup Restore — dry-run now completed 2026-07-04, see "RC Final Stabilization Closeout" at the top of this file.** Was verified by code review only (`BackupRestoreView`, `RestoreWizard` in `pages/Backup.jsx`): the UI carries an explicit, correctly-worded warning ("This action will overwrite all existing data... cannot be undone") and the backend performs a genuine full-data overwrite. It was **not executed live** against this shared instance, since doing so would destroy the demonstrable state built during this verification pass (and any other real data) with no isolated/disposable environment available here. Recommend one dry-run restore in a disposable environment (e.g. a throwaway DB copy or container) before final release sign-off.

## RC Stabilization: UI Workflow Completeness & Consistency Pass (from UI/UX Audit, `docs/UI_UX_AUDIT.md`)

### High
- BUG-021: Payments page had no direct entry point to record a payment — the only path in was via an occupant's profile page or the Explorer panel (BUG-014). Added a "Record Payment" button on `Payments.jsx` that opens a new `OccupantPickerDialog` (search + list, reusing the existing `useOccupants` hook, filtered to `status: 'active'`) so the user isn't dead-ended; selecting an occupant opens the existing, unmodified `RecordPaymentDialog` (the same component already used on `OccupantDetail.jsx`) with that occupant. On success, both the payments list and the overdue-balances widget on the same page are refetched. No second payment flow was created — `OccupantPickerDialog` only handles selection; `RecordPaymentDialog` is reused as-is. Verified live end-to-end (Playwright): opened picker, searched, selected an occupant, submitted a real test payment, confirmed it appeared instantly in the table, "Total Collected" and "Overdue Balances" updated, and the standard success toast fired — then deleted the test payment/receipt to leave no residue. Files: `frontend/src/components/OccupantPickerDialog.jsx` (new), `frontend/src/pages/Payments.jsx`.
- BUG-022: Dashboard's Recent Payments table always showed "—" for Property. Root cause: `PaymentSerializer` (`/api/payments/`) has no property/unit relation at all — `Payment` only FKs to `student`; the only place property/unit info exists for a payment is its associated `Receipt`, which snapshots `property_name`/`unit_name` as plain stored strings at payment-recording time (see `apps/payments/models.py`). Added a `property_name` `SerializerMethodField` to `PaymentSerializer` reading `obj.receipt.property_name`, matching the existing `receipt_id`/`receipt_number` method-field pattern already in that serializer (try/except `ObjectDoesNotExist`, additive only, no calculation logic touched). `Dashboard.jsx` now shows the real property name when present, falling back to "Property not specified" (via `p.property_name || 'Property not specified'`) when genuinely absent. Confirmed via live data that "genuinely absent" is a real case here: two seeded payments have receipts with empty-string `property_name`/`unit_name` because the student had no active unit assignment at the time the payment was recorded — not a bug, a real historical gap. Files: `backend/apps/payments/serializers.py`, `frontend/src/pages/Dashboard.jsx`.

### Medium
- BUG-023: `formatUGX` was duplicated across the app with inconsistent decimal handling. Two files had their own local re-implementation of the function: `components/dashboard/PaymentTable.jsx` (byte-identical to the shared `utils/format.js` version — pure duplication, no behavior difference) and `components/explorer/UnitDetailPanel.jsx` (forced `minimumFractionDigits: 2` always, e.g. "UGX 500,000.00", inconsistent with every other currency display in the app which shows no trailing decimals for whole amounts). Beyond the two named functions, seven more files inlined the same `'UGX ' + Number(x).toLocaleString('en-UG')` expression by hand instead of using the shared utility. Hardened `utils/format.js`'s `formatUGX` to coerce its argument via `Number()` and guard `NaN` (returns `—`), then replaced every local implementation and inline duplicate with the shared import. Files: `frontend/src/utils/format.js`, `frontend/src/components/dashboard/PaymentTable.jsx`, `frontend/src/components/explorer/UnitDetailPanel.jsx`, `frontend/src/pages/Payments.jsx`, `frontend/src/pages/Receipts.jsx`, `frontend/src/pages/Administration.jsx`, `frontend/src/pages/OccupantDetail.jsx`, `frontend/src/components/PaymentDetailDialog.jsx`.
  - **Verification per site:** all replacements produce byte-identical output to before, since every prior call site already numerically evaluated to `'UGX ' + Number(x).toLocaleString('en-UG')` and the hardened `formatUGX` computes exactly that.
  - **One exception (intentional display change):** `UnitDetailPanel.jsx`'s unit prices and occupant balances (`semester_price`, `monthly_price`, `occ.balance`) now render without forced 2-decimal places (e.g. "UGX 500,000" instead of "UGX 500,000.00"). This is the case the task called out to state explicitly: the old 2-decimal-forced version was the inconsistency being fixed, not the correct baseline — new output now matches every other currency display in the app.
- BUG-024: `EmptyState.jsx`'s 6 icons used `strokeWidth={1}` while the rest of the app's icons (25 occurrences across Sidebar, Topbar, Dashboard, dialogs, etc.) use `strokeWidth={1.5}`. Changed all 6 to `1.5` to match the majority convention. File: `frontend/src/components/EmptyState.jsx`.
- BUG-025: `Topbar.jsx`'s breadcrumb fell back to the generic, content-free label "Page" for any unmapped route. Replaced with a label derived from the actual last URL path segment (hyphens/underscores → spaces, title-cased), so an unmapped route now shows something meaningful instead of a placeholder. File: `frontend/src/components/Topbar.jsx`.

### Dead code removed (Task 4 — see full classification below)
- `frontend/src/pages/Settings.jsx`, `frontend/src/components/dashboard/ActionCard.jsx`, `frontend/src/components/dashboard/ActivityCard.jsx` — each confirmed CONFIRMED UNUSED via full-codebase reference search (no imports, no routes including the catch-all, no dynamic `import()`, no tests; this project has no Storybook config) — deleted.

### Discovered, not fixed (out of scope for this pass)
- `StatisticCard.jsx` (Dashboard's "Occupied Beds" card) renders `<p><span>{secondary.value}</span></p>`, and during the loading state `secondary.value` is a `<Skeleton>` which renders a `<div>` — a `<div>` nested inside a `<p>` is invalid HTML and produces a React console warning ("cannot be a descendant of") during the brief loading window. Confirmed pre-existing and unrelated to any of this pass's changes (file not touched). Flagging rather than fixing since it wasn't in scope and the fix (restructuring `StatisticCard`'s markup) touches shared UI beyond the 5 scoped tasks.

## RC Stabilization: Responsive UI Consistency Pass (from UI/UX Audit, `docs/UI_UX_AUDIT.md`)

Reference pattern established in `Occupants.jsx` and applied consistently below: table wrapped in `overflow-x-auto` as a fallback; identity column, the status-type column, and the Actions column are never `hidden`; secondary columns are tiered strictly by left-to-right position — the first secondary column gets `hidden sm:table-cell` (≥640px), the next `hidden md:table-cell` (≥768px), the next `hidden lg:table-cell` (≥1024px), so column order equals reveal order. `th`/`td` pairs always carry matching classes.

### Medium
- BUG-017: `Payments.jsx`'s table (7 columns: Occupant, Amount, Date, Method, Reference, Notes, action) and `Receipts.jsx`'s table (7 columns: Receipt #, Occupant, Amount, Date, Method, Unit, action) had no responsive column hiding, unlike `Occupants.jsx`, forcing horizontal scroll through all columns on mobile. Applied the reference pattern: Payments keeps Occupant/Amount/action always visible and tiers Date→sm, Method→md, Reference→lg, Notes→xl (a 4th tier added beyond Occupants' 3, since Tailwind's default `xl` breakpoint was already available and unused — not a new pattern, just one more rung of the same ladder); Receipts keeps Receipt #/Occupant/Amount/action always visible and tiers Date→sm, Method→md, Unit→lg. Note: Payments has no "status" field on an individual payment record (payments aren't pending/failed in this domain model), so the always-visible set there is Occupant/Amount/action rather than the "occupant, amount, status" spec — flagging the deviation since no such field exists to preserve. Files: `frontend/src/pages/Payments.jsx`, `frontend/src/pages/Receipts.jsx`.
- BUG-018: `Administration.jsx`'s 6-tab nav (`Properties, Sections, Units, Pricing, Users, Audit Log`) had no overflow handling and would clip/wrap awkwardly on narrow screens. Fixed with horizontal scroll (`overflow-x-auto` on the wrapping `<div>`, `flex-shrink-0 whitespace-nowrap` on each tab button) rather than wrapping, matching the app's one established overflow convention (every data table already uses `overflow-x-auto`; no wrap-based nav pattern exists anywhere else in the app). Also added `role="tablist"`/`role="tab"`/`aria-selected` to match the existing tab semantics already used on the Occupants status filter. Verified via Playwright at 375px: nav scrolls, and clicking the off-screen "Audit Log" tab scrolls it into view and correctly marks it active. File: `frontend/src/pages/Administration.jsx`.
- BUG-019: `UnitTab` (7 columns: Name, Section, Capacity, Status, Semester, Monthly, action) and `UsersTab` (6 columns: Username, Name, Email, Role, Status, action) inside `Administration.jsx` had no responsive hiding. Applied the reference pattern to both, keeping identity + Status + action always visible in each. `PropertyTab`, `SectionTab`, and `PricingTab` (each only 3-4 columns, already narrow) were checked and left unchanged — they already satisfy "no squeeze at mobile width" without modification, and touching them wasn't part of the observed 6-7 column problem. File: `frontend/src/pages/Administration.jsx`.
- BUG-020: `AssignOccupancyDialog.jsx` used a fixed `grid-cols-2` (no responsive prefix) for both the confirmation summary and the Start Date/Billing Mode fields, cramped at the smallest phone widths (measured: at a 375px viewport the modal's content area is ~295px, leaving ~141px per column). Changed both to `grid-cols-1 sm:grid-cols-2`, the same breakpoint already used identically for this exact "label/value pairs and form fields" layout in `OccupantDetail.jsx`'s field grid and the Payments/Receipts filter forms. Desktop is unaffected since the modal is `max-w-lg` and the `sm` (640px) breakpoint is reached well before that cap. Verified via Playwright at 375px (both fields now single-column, no page-level overflow) and at 1280px (unchanged 2-column layout). File: `frontend/src/components/AssignOccupancyDialog.jsx`.

### High
- BUG-014: Explorer's per-occupant "Record Payment" button (`components/explorer/UnitDetailPanel.jsx`) called the exact same handler as "View Occupant", silently routing the user to the occupant profile instead of a payment form. There is no dedicated payment-recording route — `RecordPaymentDialog` only exists on `OccupantDetail.jsx`. Fixed by routing to `/occupants/:id?recordPayment=1` (same query-param pattern already used by `Payments.jsx`'s `?student=` filter) and having `OccupantDetail.jsx` auto-open the existing `RecordPaymentDialog` when that param is present, then clearing it from the URL. No new payment workflow was created. Files: `frontend/src/components/explorer/UnitDetailPanel.jsx`, `frontend/src/pages/OccupantDetail.jsx`.
- BUG-015: `Sidebar.jsx` hardcoded a fake identity ("Property Manager" / manager@karosl.com / "PM" avatar) in its footer, while `Topbar.jsx` correctly showed the real authenticated user via `authService.getUser()` — the two pieces of chrome disagreed about who was logged in. Fixed by reusing the existing `authService.getUser()` (no second user-state system introduced), matching `Topbar`'s initials logic, with a "Signed in user" / "Account" fallback when user data is unavailable. File: `frontend/src/components/Sidebar.jsx`.
- BUG-016: `Properties.jsx` hardcoded the literal text "Active" in the Status column for every property regardless of actual state. Root cause was deeper than the frontend: `PropertyExplorerSerializer` (the API backing this page, `/api/properties/explorer/`) never exposed the model's existing `is_active` field at all, so there was no real field for the frontend to read. Added `is_active` to `PropertyExplorerSerializer.Meta.fields` (additive, no business logic changed) and replaced the hardcoded text with an Active/Archived badge driven by `property.is_active`, styled to match the existing Active/Past badge convention used in `OccupantDetail.jsx`. Files: `backend/apps/properties/serializers.py`, `frontend/src/pages/Properties.jsx`.

### Discovered while fixing BUG-016 (not fixed — out of scope for a small/safe UI fix)
- `PropertyExplorerView.get_queryset()` (`backend/apps/properties/views.py`) filters `Property.objects.filter(is_active=True)` unconditionally. Properties *can* be archived today (Administration → Properties tab → Archive, via `AdminPropertyViewSet`), but once archived they disappear entirely from `/api/properties/explorer/` — meaning they vanish from both the Properties page and the Property Explorer tree rather than showing as "Archived". The badge fix above is correct and will render properly the moment this endpoint returns an inactive property, but doing so requires deciding whether Properties/Explorer should show archived properties at all (and how), which is a product/business-logic decision beyond this stabilization pass.

## Frontend Auth & Layout Pass (closes previously-deferred "Frontend login/logout UI")

### High
- BUG-012 (closes previously-deferred "Frontend login/logout UI is not implemented"): The app had no login route, form, logout action, or auth guard — `App.jsx` routed straight to the Dashboard regardless of session state, so every API call went out with no token and failed with "Authentication credentials were not provided" as soon as a user interacted with the page. Added `pages/Login.jsx`, `services/auth.js` (token/user storage against the existing, already-working `/api/auth/login/` and `/api/auth/logout/` endpoints), and `components/RequireAuth.jsx` guarding all routes except `/login`. The API client (`services/api.js`) now also clears the session and redirects to `/login` on any `401` response, instead of leaving the user on a dead-end error. `Topbar` now shows the real logged-in username and a working logout button (was hardcoded "Property Manager"/"PM"). Verified live via Playwright: logged in, screenshotted the Dashboard and Payments pages. Files: `frontend/src/pages/Login.jsx`, `frontend/src/components/RequireAuth.jsx`, `frontend/src/services/auth.js`, `frontend/src/services/api.js`, `frontend/src/components/Topbar.jsx`, `frontend/src/App.jsx`.
- BUG-013: On desktop viewports (`lg:` breakpoint, e.g. a maximized window), the main content area rendered with a large blank gap between the sidebar and the actual page content. Root cause: `Sidebar` switches from `fixed` positioning (mobile, off-canvas) to `static` (desktop, in normal document flow) at the `lg:` breakpoint, but the content `<div>` in `layouts/MainLayout.jsx` still carried `lg:pl-64` — padding originally added to offset a *fixed* sidebar. Once the sidebar became `static` and started occupying real horizontal space itself, that leftover padding double-reserved space for it, and with no flex row to place them side by side, content ended up pushed an extra 16rem to the right of where the (now in-flow) sidebar ended. Fixed by making the outer layout container `lg:flex` and changing the content pane to `lg:flex-1 lg:min-w-0`, so the sidebar and content sit correctly in a flex row at desktop widths; mobile/tablet (off-canvas sidebar) is unaffected. Verified visually with real screenshots at 1920x1080. File: `frontend/src/layouts/MainLayout.jsx`.

## Backend API Stability Sweep (Release Candidate)

### Critical
- BUG-006: Occupant creation returned HTTP 500 (`IntegrityError: UNIQUE constraint failed: occupants_student.student_id_number`) whenever a second occupant was created with a blank `email`, `student_id_number`, or `national_id`, because those fields were `unique=True, blank=True` without `null=True`, and DRF's `CharField.run_validation` skips the auto-generated `UniqueValidator` for blank input, so duplicate empty strings reached the DB unchecked. Root cause verified live via `POST /api/occupants/` reproduction against a scratch copy of the dev DB. This exact fix already existed, reviewed and tested, on the `dev` branch (commit `0554017`, "Fix BUG-003") but was never merged into this branch — ported the vetted migration (`0003_add_null_to_unique_fields`), model, and serializer changes here instead of re-deriving a new one. Files: `apps/occupants/models.py`, `apps/occupants/serializers.py`, `apps/occupants/migrations/0003_add_null_to_unique_fields.py`, `apps/occupants/tests.py`.
- BUG-007: The `occupancy.0004_allow_same_day_checkout` migration existed on disk (relaxing the `ck_occupancy_end_date_after_start` CHECK constraint to allow `end_date == start_date`) but had never been applied to the dev database — `showmigrations` showed it unapplied. Same-day checkout therefore still crashed with `CHECK constraint failed`. Applied the pending migration.
- BUG-008: Recording a payment for an occupant with no `student_id_number` (now possible after BUG-006's fix) crashed with `IntegrityError: NOT NULL constraint failed: payments_receipt.student_id_number`, because `Receipt.student_id_number` is `NOT NULL` at the DB level but `PaymentService._generate_receipt` copied the student's (now possibly `None`) `student_id_number` straight across. Reproduced live via `POST /api/payments/`. Fixed by coercing `None` to `""` when snapshotting onto the receipt. File: `apps/payments/services/__init__.py`.
- BUG-009: `PaymentService.record_payment` saved the `Payment` row and generated its `Receipt` as two separate, non-atomic writes. Reproduced live: an earlier failed payment attempt (crashing during receipt generation, per BUG-008) left an orphaned `Payment` row with no `Receipt` permanently in the database, invisible to the user (who saw only a 500) and only discoverable via a balance discrepancy. Wrapped both writes in `transaction.atomic()` so a receipt-generation failure rolls back the payment too. File: `apps/payments/services/__init__.py`.

### High
- BUG-010: `OccupancyService.checkout_occupancy`'s "already closed" guard checked the `is_active` field instead of `end_date`, and `update_occupancy` (`PATCH /api/occupancy/<id>/`) never kept `is_active` in sync when `end_date` was set directly. Reproduced live: PATCHing `end_date` on an active occupancy left `is_active=True` (stale), and a subsequent `checkout` call then re-ran the checkout logic and crashed with `CHECK constraint failed: ck_occupancy_end_date_after_start`. Fixed `checkout_occupancy` to check `end_date is not None`, and `update_occupancy` to set `is_active = (end_date is None)` whenever `end_date` is part of the patch. Files: `apps/occupancy/services/__init__.py`, `apps/occupancy/tests.py`.
- BUG-011 (closes previously-deferred "export header/key mismatches"): CSV/XLSX export column headers are matched to row-dict keys via a lossy `header.lower().replace(" ", "_")` transform in `ExportService._write_csv`/`_write_xlsx`. Three of the four exports had headers that didn't actually match their row dict keys, so those columns were silently blank in every exported file: `export_occupants` ("Student ID" vs. key `student_id_number`, "Active" vs. `is_active`), `export_occupancies` ("Active" vs. `is_active`), `export_receipts` ("Outstanding Balance" vs. `outstanding_balance_after`, "Unit" vs. `unit_name`, "Property" vs. `property_name`). Reproduced live via `GET /api/backups/export/?entity=...`. Fixed by aligning row dict keys to the existing header-derived keys (via `.values(..., student_id=F("student_id_number"), active=F("is_active"))` for occupants, and renamed dict keys for occupancies/receipts) — no header text or API shape changed, only the previously-blank data now populates correctly. File: `apps/backup/services.py`.

### Verified clean (reproduced live, no bug found)
- Creating properties and confirming immediate visibility in `/api/properties/`, `/api/properties/explorer/`, and `/api/admin/properties/` — works correctly.
- Dashboard `/api/occupancy/summary/` `total_students`/`active_students`/`total_occupied` correctly reflect live data after creating occupants and assigning occupancy — the previously-documented "Dashboard 0/0" fix holds.

## Fixed In Current Debugging Pass

### Critical
- Admin APIs allowed any authenticated user because admin viewsets used `IsAuthenticated | IsPropertyManager`. Fixed by requiring `IsPropertyManager`, which already allows superusers and authenticated Property Manager group members.
- Pricing rule create/delete returned HTTP 500 because audit descriptions referenced serializer-only `unit_name` on the model. Fixed by reading `rule.unit.name`.
- Backup restore failed for foreign-key records because serialized FK values were assigned to model descriptors as integers. Fixed by restoring FK values through their `<field>_id` columns.
- Occupancy assignment ignored effective pricing rules. Fixed by using `AdminService.get_effective_price()` when snapshotting assignment price.
- Monthly balances used a one-month snapshot as the full charge forever. Fixed balance calculation so monthly snapshots are treated as monthly rates and multiplied by elapsed months.

### High
- Admin user create/update failed when `groups` were submitted because the service assigned many-to-many fields directly. Fixed by popping groups and applying `user.groups.set(...)` after save.
- Admin user create allowed submitted `is_superuser`. Fixed by making `is_superuser` read-only in admin user serializers.
- Same-day checkout failed validation because `end_date` had to be greater than `start_date`. Fixed validation and DB constraint to allow same-day checkout.
- Payment serialization could crash for payments without receipts. Fixed receipt fields to return `null` when the reverse receipt does not exist.
- Occupant detail could crash while loading payment summary because `CardSkeleton` was rendered without import. Fixed missing import.
- Global Search called double-prefixed and unregistered API paths and expected the wrong explorer response shape. Fixed endpoint paths and bare-list explorer handling.
- Explorer unit detail panel was unreachable because unit selection was not passed through `PropertyNode` and `SectionNode`. Fixed callback wiring.
- Receipt PDF links bypassed token authentication. Fixed by fetching PDFs as authenticated blobs through the API client.
- Payment workflow crashed with `useState is not defined` whenever a payment's detail view was opened because `PaymentDetailDialog` used `useState` without importing it from `react`. Fixed missing import and added a regression test.

### Release Workflow QA
- Dashboard occupant totals did not update after registering occupants because `/api/occupancy/summary/` did not return `total_students` or `active_students`, while the Dashboard page read those fields. Fixed the API response and added coverage.
- The Properties page never displayed created properties because it was a static placeholder. Fixed it to render the existing property hierarchy API in a read-only overview.
- Administration unit creation could submit from the `All Sections` state without a required `section`. Fixed by disabling `Add Unit` until a section is selected.

## Deferred
- Report generation is not implemented. The Reports route loads a placeholder with disabled report cards and a link to Backup & Export. This requires a reporting feature pass, so it was not added during bug fixing.
- Medium/low-priority audit findings remain queued for a separate pass: invalid pagination handling, backup history pagination UI, admin section reorder awaiting, guided tour positioning, and additional UX polish. (Export header/key mismatches, previously listed here, were fixed in the Backend API Stability Sweep — see BUG-011.)

## Fixed 2026-08-24 — Permission gap on business endpoints (`IsAuthenticated | IsPropertyManager` no-op)

`IsAuthenticated | IsPropertyManager` was used on 12 write-capable and read-only viewsets across 8 apps
(occupants, occupancy, payments, properties, sections, units, dashboard, reports). Because `IsAuthenticated`
alone already grants access to any logged-in user, the OR gate had no actual effect — any authenticated user
could create occupants, record payments, and modify occupancy regardless of group membership.

**Decision and fix (2026-08-24):**
- **Write-capable endpoints** (occupants, occupancy, payments): tightened to `[IsPropertyManager]` only.
  This matches the precedent already applied to `apps/administration` and closes the audit's top blocker.
- **Read-only endpoints** (properties, sections, units, dashboard, reports): simplified to `[IsAuthenticated]`
  only (removed the no-op OR). Viewing data was not the audit's concern; restricting reads could break
  normal staff workflows.
- **No per-property scoping model exists** (no `role` field, no user↔property relationship) — "Property Manager"
  is a single global Django group. A more granular fix (per-property manager assignment) is out of scope
  here; this matches the existing `apps/administration` pattern and closes the audit blocker.
- **Verified safe for staging demo account `karosadmin`** — it is a Django superuser (created via
  `createsuperuser`), and `IsPropertyManager.has_permission` short-circuits True for any superuser,
  so the permission change will not lock it out.
- **Tests updated**: added the "Property Manager" group to the test user's setUp in
  `apps/payments/tests.py`, `apps/occupants/tests.py`, and `apps/occupancy/tests.py` (previously they
  relied on the no-op `IsAuthenticated` OR). Added negative-path tests in all three files asserting 403
  for a plain non-manager staff user, matching the pattern already established in `apps/administration/tests.py`.
  **All 257/257 backend tests pass** (baseline from the 2026-08-20 audit): 36 payment, 53 occupants/occupancy,
  plus administration, dashboard, audit, and other app suites.
- **Files changed**: `backend/apps/{payments,occupants,occupancy}/views.py` (permission_classes),
  `backend/apps/{properties,sections,units,dashboard,reports}/views.py` (remove no-op `| IsPropertyManager`),
  `backend/apps/{payments,occupants,occupancy}/tests.py` (setUp group + negative tests).
